document.addEventListener('DOMContentLoaded', function () {

    // const form = document.querySelector("form");

    const nameDescription = document.getElementById("name");
    console.log(nameDescription);
    const weekNumber = document.getElementById("nb");
    console.log(weekNumber);
    const saveButton = document.querySelector("button");
    console.log(saveButton);

    // saveButton.addEventListener("submit", ev => {
    //     e.preventDefault();
    //
    //     //jeżeli wszystko ok to wysyłamy
    //     if (nameDescription.value.length <= 50 && weekNumber.value <= 54) {
    //
    //     }
    // }
})